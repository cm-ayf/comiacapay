mutation UpsertDisplay(
  $guildId: ID!
  $eventId: ID!
  $itemId: ID!
  $input: UpsertDisplay!
) {
  upsertDisplay(
    guildId: $guildId
    eventId: $eventId
    itemId: $itemId
    input: $input
  ) {
    itemId
    price
  }
}
