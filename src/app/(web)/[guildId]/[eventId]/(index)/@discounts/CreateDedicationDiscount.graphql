mutation CreateDedicationDiscount(
  $guildId: ID!
  $eventId: ID!
  $input: CreateDedicationDiscount!
) {
  createDedicationDiscount(
    guildId: $guildId
    eventId: $eventId
    input: $input
  ) {
    __typename
    id
    itemId
  }
}
