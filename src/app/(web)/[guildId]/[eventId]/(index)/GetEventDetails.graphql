query GetEventDetails($guildId: ID!, $eventId: ID!) {
  event(guildId: $guildId, id: $eventId) {
    id
    name
    date

    discounts {
      ... on SetDiscount {
        __typename
        id
        itemIds
        amount
      }

      ... on DedicationDiscount {
        __typename
        id
        itemId
      }
    }

    displays {
      price

      item {
        id
        name
        picture
      }
    }

    guild {
      name

      me {
        register
        write
      }

      items {
        id
        name
        picture
        issuedAt
      }
    }
  }
}
