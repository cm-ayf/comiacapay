query GetEventDetails($guildId: ID!, $eventId: ID!) {
  event(guildId: $guildId, id: $eventId) {
    id
    name
    date

    discounts {
      ... on SetDiscount {
        __typename
        id
        itemIds
        amount
      }
    }

    displays {
      price

      item {
        id
        name
        picture
      }
    }

    guild {
      name

      me {
        register
        write
      }

      items {
        id
        name
        picture
        issuedAt
      }
    }
  }
}
