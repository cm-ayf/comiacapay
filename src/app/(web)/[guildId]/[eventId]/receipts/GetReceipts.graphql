query GetReceipts($guildId: ID!, $eventId: ID!) {
  event(guildId: $guildId, id: $eventId) {
    receipts {
      id
      total

      records {
        itemId
        count
        dedication
        internal
      }
    }
  }
}
