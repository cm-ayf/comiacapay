type Item {
  id: ID! @model

  guild: Guild!

  name: String! @model
  picture: String @model
  issuedAt: Date! @model
}

input CreateItem {
  name: String!
  picture: String
  issuedAt: Date!
}

input UpdateItem {
  name: String
  picture: String
  issuedAt: Date
}
